import SwiftUI

struct LearnView: View {
    var body: some View {
        NavigationView {
            Text("Courses")
                .foregroundColor(AppTheme.ink)
                .navigationTitle("Learn")
        }
    }
}
