import SwiftUI

struct ExamView: View {
    var body: some View {
        NavigationView {
            Text("Mock Exams")
                .foregroundColor(AppTheme.ink)
                .navigationTitle("Exam")
        }
    }
}
