node_modules/
.next/
dist/
*.log
.DS_Store
coverage/
.worktrees/

# Next.js generated
next-env.d.ts
*.tsbuildinfo

# Prisma local databases
prisma/*.db
prisma/*.db-journal

# Env files — never commit secrets. Only .env.example (the template) is tracked.
.env
.env.*
!.env.example

.vercel

# Sentry
.sentryclirc
.env.sentry-build-plugin

# Database dumps — may contain user/payment data, never commit
*.dump
