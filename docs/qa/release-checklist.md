# 发布准入清单（Release Sign-off Checklist）

| 项 | 内容 |
|---|---|
| 文档版本 | v1.0 |
| 最后更新 | 2026-08-24 |
| 责任角色 | QA（给出结论）· PO（做出决策） |

---

## 发布流程

```
代码冻结 → 全量回归 → 缺陷收敛 → 准入检查表 → Go/No-Go → 发布 → 生产冒烟 → 观察期
                ↑                                    │
                └──────── No-Go：修复后重新回归 ◄─────┘
```

**"缺陷收敛"不等于零缺陷**，而是：**没有未决的 P0/P1，且全部剩余缺陷已被明确接受并记录在案。**

---

## 1. 准入检查表

### 1.1 缺陷状态

- [ ] 所有 **P0 / P1 缺陷已修复，并经 QA 验证**
      > ⚠️ 开发说"修好了"不算。必须由 QA 在与缺陷单相同的环境和步骤下确认。
- [ ] 剩余 P2 / P3 缺陷已**逐条列出**
- [ ] 剩余缺陷已由 **PO 明确接受**（accepted risk），且**留痕**
- [ ] 本次修复的缺陷已完成**回流**（补了自动化用例 / 更新了回归清单）

### 1.2 自动化与构建

- [ ] `pnpm test` 全部通过，**无跳过、无重跑**
- [ ] 任何失败用例都有**逐条解释**（不接受"重跑一次就好了"）
- [ ] `pnpm typecheck` 通过　　✅ 2026-08-25 起已进 CI 门禁
- [ ] `pnpm build` 通过　　　　✅ 2026-08-25 起已进 CI 门禁
      > 拦的是 vitest 抓不到的一类：Server/Client 边界错误。**能力边界已实测**（2026-08-25 用 throwaway PR #43 验收）：
      > 抓得到「Server Component 里用 React hook」和「Client Component 里 import `next/headers`」；
      > **抓不到**「Server Component 里裸用 `window`」—— 本应用每条路由都编译为 ƒ（按需渲染），构建期不预渲染，那段代码根本没被求值，只会在运行时 500。
- [ ] `pnpm e2e` 通过　　　　　✅ 2026-08-25 起已进 CI（独立 job）
      > 只覆盖预约链路一条旅程（登录 → 预约 → 取消 → 券回池 → 改约）。**购买链路与考试链路仍是零 E2E**，见 automation-roadmap P2。
- [ ] `pnpm db:verify` 通过（对着**将要部署的那个库**跑，不是本地测试库）
      > 它会先打印 `→ target:`。确认那一行是要发布的库 —— 默认读 `.env`，而开发机的 `.env` 里是生产。
- [ ] 无新增的 flaky test；已隔离的 flaky test 均有跟踪单

### 1.3 回归

- [ ] [`regression-checklist.md`](./regression-checklist.md) §1 核心路径全部通过
- [ ] 按 §2 变更影响映射确定的范围已全部回归
- [ ] 假失败率 < 20%

### 1.4 核心用户旅程（手工）

- [ ] 注册 → 购买 → 课程解锁
- [ ] 开始考试 → 作答 → 交卷 → 结果 → 错题回顾
- [ ] 预约 Flight Review → 改期 → 取消
- [ ] 游客 10 题体验完整走通
- [ ] EN 与 ZH 各走一遍上述旅程

### 1.5 支付专项 💰

> 涉及支付的发布，本节**逐项不可跳过**。

- [ ] Stripe 测试卡三条路径验证：
      - [ ] 支付成功 `4242 4242 4242 4242`
      - [ ] 支付被拒 `4000 0000 0000 0002`
      - [ ] **3DS 验证** `4000 0025 0000 3155`
- [ ] 用户主动取消结账 → 不产生记录、不发权益
- [ ] **Webhook 幂等**：重放同一事件不重复发放权益
- [ ] **⚠️ 生产环境的 Stripe LIVE key 与 LIVE price id 属于同一 Stripe 模式**
      > 这是本项目最容易发生的生产事故。`.env` 用 TEST 模式，`.env.production` 用 LIVE 模式，**两套模式的 price id 不通用**。配错了代码完全正确，但用户点下付款的瞬间失败。**没有任何自动化测试能提前发现它——因为问题不在代码里，在配置里。**
- [ ] 生产 Webhook Endpoint 已配置，签名密钥正确

### 1.6 权限与安全

- [ ] [`permission-matrix.md`](./permission-matrix.md) 中标 ✅ 的格子对应测试全部通过
- [ ] 交卷前答案不下发（Web + **移动端**）
- [ ] 检查点接口无法解析考试题目 ID（`SEC-04`）
- [ ] 非管理员访问 `/coriander` 全部返回 404
- [ ] 若本次改动涉及权限逻辑 → **完整权限矩阵重跑**

### 1.7 数据与配置

- [ ] 数据库迁移已在**生产数据副本**上演练
- [ ] 迁移有**回滚方案**，且已确认回滚后的数据处理方式
- [ ] 生产环境变量已全部配置（对照 `.env.example` 逐项核对）
- [ ] **题库健康度检查**：各难度题量足以生成完整试卷
      > ⚠️ 对应 PRD U1 / DEF-003。特别确认 `difficulty === 1` 的 Basic 题目 **≥ 35 道**，否则免费用户会拿到题量不足的试卷。
      > CMS 的 `/coriander/questions` 顶部已有健康度表，直接看红色行。

#### 1.7.1 Schema 部署步骤（**有序**，PRD U13 首次上线必做）

`prisma db push` 只能创建 Prisma schema 能表达的东西。以下步骤在它的能力之外，**漏跑没有任何症状**：

```bash
1. pnpm db:push      # 同步 schema
2. ALLOW_REMOTE_DB_WRITE=1 pnpm db:indexes
                     # 应用 prisma/sql/*：RLS 加固 + 部分唯一索引
                     # ⚠️ 漏跑索引 = 「一人一约/一时段一约」从数据库保证降级为应用层判断，只在并发下暴露
                     # ⚠️ 漏跑 RLS  = Supabase 的 anon/authenticated 角色不再默认被拒（应用本身无感）
3. pnpm db:verify    # 门禁：不通过（非零退出）就不要切流量
4. pnpm exec tsx scripts/migrate-flight-review-credits.ts --dry-run   # 先看将发放的券数，确认数字合理
5. pnpm exec tsx scripts/migrate-flight-review-credits.ts             # ⚠️ 漏跑 = 全部已付费用户静默失去已购的预约资格
6. pnpm db:verify    # 再确认：pendingCreditGrants 必须为 0
```

- [ ] 上述 6 步已按顺序执行，两次 `db:verify` 均通过
      > **不要依赖 CI 的绿灯**：`vitest.globalSetup` 会自己补建这些索引，所以 CI 无论生产是否跑过这一步都是绿的。这正是 `db:verify` 存在的原因。
- [ ] 每一步开头打印的 `→ target: <host>/<db>` 确认是**要发布的那个库**
      > `db:indexes` / `db:verify` / 券迁移脚本都会先打印目标。两个会写库的对非本机目标默认**拒绝执行**，必须显式加 `ALLOW_REMOTE_DB_WRITE=1`。
- [ ] **目标库用内联环境变量传，不要写进 `.env` 或 `.env.production`**
      > Prisma CLI 只加载 `.env`（不加载 `.env.production`），所以不加前缀的 `pnpm db:push` 打的是 `.env` 里那个库 —— 它会成功、会打印 "in sync"，而目标库一个字没动。正确写法：
      > ```bash
      > export PROD_POOLED="postgresql://...:6543/postgres?pgbouncer=true"
      > export PROD_DIRECT="postgresql://...:5432/postgres"   # db push 走直连，不走 pgbouncer
      > DATABASE_URL="$PROD_POOLED" DIRECT_URL="$PROD_DIRECT" pnpm exec prisma db push
      > ```
      > 写进文件的风险是它会被后续命令静默继承 —— 例如 `next start` 在 `NODE_ENV=production` 下会加载 `.env.production`。
- [ ] **从有新 schema 的检出上跑**（发布分支，不是 `main`）
      > `prisma db push` 读的是当前工作区的 `prisma/schema.prisma`。在尚未合并的 `main` 上跑，等于把旧结构推回目标库。
- [ ] 迁移脚本的 dry-run 数字与预期一致（应约等于「持 `flight_review` 权益的人数 + `accessTier=PAID` 的人数」）

### 1.8 内容与国际化

- [ ] 新增文案的 EN 与 ZH **均存在**，无缺失键
- [ ] 新增课时的双语正文均已填写
- [ ] 错误提示无未翻译的技术性文案（如直接暴露 `payment_required`）

### 1.9 可观测性

- [ ] Sentry 能收到新版本的事件
      > **发布后确认，不是发布前假设。**
- [ ] 关键接口的错误日志可正常查询

---

## 2. Go/No-Go 会议

### 2.1 QA 该说什么

**不是"测完了，可以发"。**那五个字是新手 QA 与资深 QA 最明显的分水岭。

正确的陈述结构是：**做了什么 → 没做什么 → 风险是什么 → 我的建议 → 需要谁拍板**

#### 陈述模板

```
本轮共执行 __ 条用例，通过 __ 条，通过率 __%。

【已测范围】
- A、B、C

【未测范围及原因】
- D（时间不足）
- E（环境不具备）

【已知遗留缺陷】
- 共 __ 个，其中 P2 __ 个、P3 __ 个
- 最坏情况下的影响是：______

【我的建议】
建议发布 / 建议延期 / 建议关闭 X 功能后发布

【需要确认】
请 @PO 确认是否接受上述 __ 个 P2 缺陷的风险。
若决定发布，建议同时准备好 ______ 的回滚方案。
```

### 2.2 QA 有没有否决权？

**大多数情况下没有绝对否决权，但有"记录在案的反对"。**

这不是弱势，而是正确的责任划分——发布是商业决策，需要权衡收入、竞品、对客户的承诺；**QA 掌握的是质量信息，不是全部信息**。

因此 QA 在 Go/No-Go 上正确的做法是：

> **不阻拦决策，但确保决策者在知情的前提下做决定，并且这个知情过程留下痕迹。**

这既是职业素养，也是自我保护——出事之后，"当初没人告诉我"是最伤人的一句话。

### 2.3 临近发布发现严重缺陷

不隐瞒、不夸大。第一时间**一次性给出三样东西**：

1. **影响面** —— 多少用户、有无数据损坏、有无资金或合规风险
2. **是否有绕过办法** —— 用户能自救？客服能处理？能用开关关掉？
3. **修复 + 验证需要多久** —— **必须包含验证时间**

然后直接列出三个选项：**延期发布** / **带缺陷发布 + 公告绕过方案** / **关闭该功能后发布**。

---

## 3. 发布后验证（生产冒烟）

> **测试环境全绿不能证明生产可用。**两个环境存在三类系统性差异：**配置不同、数据不同、第三方服务的模式不同**。

### 3.1 立即执行（发布后 15 分钟内）

- [ ] **`curl -i https://<域名>/api/health/schema` 返回 200**
      > 503 表示部署漏了 §1.7.1 的某一步，响应体会指明是哪一步。这是最先该看的一项 —— 它比任何手工路径都快，且能抓到「索引没建」这种手工点击永远发现不了的问题。
- [ ] 首页在 `/en` 与 `/zh` 正常加载
- [ ] 登录功能正常
- [ ] 创建一场考试并交卷，成绩正确
- [ ] **用真实卡完成一笔小额真实支付，确认权益正确发放** 💰
      > 这是唯一能拦下 Stripe 模式配置错误的手段。
- [ ] Sentry 已收到新版本的事件（证明监控在工作）
- [ ] 检查错误率是否较发布前异常上升

### 3.2 观察期（发布后 24 小时）

- [ ] Sentry 错误率处于正常水平
- [ ] 无支付失败率异常
- [ ] 无用户反馈的新增问题

---

## 4. 回滚预案

发布前必须能回答以下问题，否则不具备发布条件：

| 问题 | 本次答案 |
|---|---|
| 回滚需要多长时间？ | |
| 回滚由谁执行？ | |
| 回滚后，新版本期间产生的数据如何处理？ | |
| 数据库迁移能否回滚？若不能，如何兼容？ | **不能。见下方 ⚠️** |
| 触发回滚的判断标准是什么？ | |

> ⚠️ **本次发布的 schema 变更不可逆，回滚策略只能是「向前修复」。**
>
> N15 移除了 `FlightReviewBooking.customerId @unique`。上线后只要**任一用户取消过一次预约**，该用户就会有第二条预约记录（取消改为置 `CANCELLED` 而非删行），此时恢复该唯一约束会因唯一键冲突而失败 —— 也就是说，**回滚窗口在第一次取消发生的那一刻就关闭了**。
>
> 因此：出问题时不要回退版本，而是在当前版本上修复。这一条必须在发布前对齐，不能等事故现场才发现回不去。

> **最后一条最容易被忽略。**没有预设标准，回滚决策就会在事故现场靠情绪做出——要么拖太久，要么过早回滚导致数据更混乱。
> 建议的标准示例：核心接口错误率 > 5%，或出现任何资金/权益相关的错误。

---

## 5. 本次发布的特别提醒

DEF-001 / DEF-002 / DEF-003 **均已于 2026-08-25 修复**（见 [`README.md`](./README.md)「当前的关键结论」）。本次发布的风险已转移到**部署执行**与**残余覆盖缺口**：

### 5.1 部署执行风险（三项都是静默失败）

| # | 风险 | 后果 | 拦截手段 |
|---|---|---|---|
| 1 | 漏跑 `pnpm db:indexes` | 「一人一约 / 一时段一约」降级为应用层判断，只在并发下暴露；**且全表 RLS 不会被开启** | `pnpm db:verify` + `/api/health/schema`（§1.7.1、§3.1） |
| 2 | 漏跑券迁移脚本 | **全部已付费用户静默失去已购的预约资格** | 同上；`db:verify` 的 `pendingCreditGrants` 必须为 0 |
| 3 | 出问题想回滚 | **回不去**，唯一约束无法恢复 | 见 §4。发布前就要接受「只能向前修复」 |
| 4 | ops 脚本打错库 | `db:indexes` / `db:verify` / 券迁移脚本都默认读 `.env`，而**哪个库在 `.env` 里全凭当时的配置** | 三者都会先打印 `→ target:`；两个会写库的对非本机目标默认拒绝，需显式 `ALLOW_REMOTE_DB_WRITE=1` |

> 第 1、2 项的共同点：**CI 绿灯不能证明它们做了**。`vitest.globalSetup` 会自己补建索引与 RLS，所以测试环境永远是好的。
>
> 第 4 项不是假设，而且比一开始判断的严重得多：
>
> 2026-08-25 构建这套门禁时，`pnpm db:indexes` 在无人察觉的情况下打到了 `.env` 指向的远端库。当时我据「Stripe 是 test key、只有 4 个客户、有 `@rpas.test` 邮箱、`APP_URL=localhost`」推断那是 dev，并把这个推断写进了本文档。**推断是错的。**
>
> 2026-08-26 从 Supabase 控制台确认：两个项目的命名与引用它们的 env 文件**完全相反** ——
> `pacificdrone-prod` 在 **us-west-1**，正是 `.env` 指向的那个（活跃，就是生产）；
> `pacificdrone-dev` 在 ca-central-1，是 `.env.production` 指向的那个（已暂停，所以连不上）。
>
> 于是整轮"在 dev 上的演练"实际都跑在**生产库**上，包括一次 `db push --accept-data-loss`，删掉了 `AgentRun` / `AgentStep` / `MockTicket` 三张表共 32 行（已终止的 SDLC 实验产物，无客户/支付/产品数据）。客户数据未受影响，`db:verify` 通过。
>
> **教训不是"当时猜错了"，是"当时在猜"。** 关于目标库的判断必须来自控制台或 `→ target:` 那一行，不能来自同一个文件里其它变量长什么样。
>
> 处置：`.env` 已改为本地 Postgres；生产连接串移到 `.secrets/prod-db.env`（无人自动加载）；`.env.production` 不再存放任何 DB URL；`db:push` 也加上了目标守卫。对应关系见 `CLAUDE.md`「Which database a command talks to」。

### 5.2 残余覆盖缺口（发布决策需明确接受）

| 缺口 | 说明 |
|---|---|
| **购买链路 E2E 为 0** | 预约链路已有 Playwright 冒烟（`pnpm e2e`，已进 CI）；**支付链路仍从未整机通电**。本轮改写了支付侧的资格判定与退款流程。见 [`automation-roadmap.md`](./automation-roadmap.md) P2 |
| **考试链路 E2E 为 0** | 同上。本轮改了超时结算与题池校验 |
| **Server Component 里裸用 `window` 无门禁可拦** | `build` 抓不到（本应用不预渲染），`vitest` 也抓不到。只有 Playwright 走到的页面能覆盖。本轮新增了 6 个客户端组件并给考试入口加了 `useSession()`，属于该风险的活跃区 |
| **`FR-Only` 身份矩阵未自动化** | 该身份定义已改为「持可用审查券」，且现在**会消失**（券用完退回 `FREE`）—— 旧模型里不存在的状态迁移，目前零矩阵级覆盖。见 automation-roadmap P1 |
| 一个偶发用例 | `verificationCode.test.ts` 的并发消费用例曾失败一次。**此后连续 14 次全量跑均未复现，根因未取得** —— 这是定界，不是修复。定界依据：该用例 `now` 被钉死，无时间不确定性；消费走 `consumedAt: null` 条件更新，Postgres 行锁使「重复消费」结构上不可能。断言已改为 `allSettled` 并同时打印成功数、失败原因、异常，下次复现即可一次分清方向 |
